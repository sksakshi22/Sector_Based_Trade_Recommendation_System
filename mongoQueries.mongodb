// MongoDB Playground
// Use Ctrl+Space inside a snippet or a string literal to trigger completions.

const database = 'DBStockInfo';
const ITSector = 'ITSector';
const EnergySector = 'EnergySector';

// Create a new database.
use(database);

// Create a new collection.
//db.createCollection(ITSector);
db.createCollection(EnergySector);

  
db.ITSector.find({Name: "COFORGE LIMITED"});
